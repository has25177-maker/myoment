import streamlit as st
import pandas as pd
from datetime import date

st.set_page_config(page_title="묘멘트", page_icon="🐱", layout="wide")

# -----------------------------
# 세션 상태 초기화
# -----------------------------
if "records" not in st.session_state:
    st.session_state.records = []  # 건강 기록 리스트 (dict)

if "food_db" not in st.session_state:
    # 음식 사전 간단 DB (예시)
    st.session_state.food_db = {
        "닭가슴살": {"가능": "소량 삶아서 가능", "주의": "염분·양념 X"},
        "소고기": {"가능": "익힌 살코기 소량 가능", "주의": "양념/기름 X"},
        "돼지고기": {"가능": "익힌 살코기 소량 가능", "주의": "기름기·양념 X"},
        "참치캔": {"가능": "고양이용 참치 가능", "주의": "사람용 참치는 염분↑"},
        "연어": {"가능": "익힌 연어 소량", "주의": "생연어/훈제 X"},
        "우유": {"가능": "락토프리만 가능", "주의": "일반 우유는 설사 유발"},
        "요구르트": {"가능": "무가당만 소량", "주의": "인공감미료 X"},
        "치즈": {"가능": "무염 치즈만", "주의": "염분·유당↑"},
        "빵": {"가능": "한두 조각만", "주의": "크림·초코 금지"},
        "초콜릿": {"가능": "불가", "주의": "테오브로민 독성"},
        "양파": {"가능": "불가", "주의": "적혈구 파괴 → 빈혈"},
        "마늘": {"가능": "불가", "주의": "양파와 동일 독성"},
        "포도": {"가능": "불가", "주의": "신장 손상"},
        "건포도": {"가능": "불가", "주의": "포도와 동일 독성"},
        "사과": {"가능": "과육 소량", "주의": "씨·심 제거"},
        "수박": {"가능": "과육만 소량", "주의": "과당 주의"},
        "고구마": {"가능": "익혀서 소량", "주의": "당분↑"},
        "감자": {"가능": "익힌 감자만", "주의": "생감자 독성"},
        "브로콜리": {"가능": "삶아서 소량", "주의": "날로 많이 X"},
    }

# -----------------------------
# 유틸 함수
# -----------------------------
def add_record(rec: dict):
    st.session_state.records.append(rec)

def get_records_df():
    if not st.session_state.records:
        return pd.DataFrame()
    return pd.DataFrame(st.session_state.records)


# -----------------------------
# 페이지 1. 건강 기록 (구토 기능 포함)
# -----------------------------
def page_health_log():
    st.title("📒 건강 기록")
    st.write("버튼/선택 위주로 간단하게 오늘 상태를 기록하는 화면이에요.")

    col1, col2 = st.columns(2)

    with col1:
        rec_date = st.date_input("기록 날짜", value=date.today())

        meal = st.radio("🍽 식사량",
            ["거의 안 먹음", "평소보다 적게", "평소와 비슷", "평소보다 많이"],
            index=2)

        water = st.radio("💧 음수량",
            ["거의 안 마심", "평소보다 적게", "평소와 비슷", "평소보다 많이"],
            index=2)

        poop = st.radio("💩 배변 상태",
            ["정상", "단단함", "무름/설사", "혈변/검은 변", "안 봄"])

        # ⭐ 구토 추가
        vomit_yes = st.radio("🤮 구토 여부", ["없음", "있음"], index=0)

        vomit_color = ""
        vomit_type = ""
        vomit_amount = ""
        vomit_count = ""

        if vomit_yes == "있음":
            vomit_color = st.selectbox("구토 색깔",
                ["노란색(담즙)", "투명/거품", "갈색", "음식물", "분홍/붉은 기운", "기타"])

            vomit_type = st.selectbox("구토 종류",
                ["헤어볼", "음식토", "물토", "거품토", "이물 가능성", "기타"])

            vomit_amount = st.radio("구토 양", ["적음", "보통", "많음"], index=1)

            vomit_count = st.radio("구토 횟수(오늘)", ["1회", "2~3회", "4회 이상"], index=0)

    with col2:
        activity = st.multiselect(
            "🏃 활동 및 행동",
            [
                "평소와 비슷", "잠이 많아짐", "활동이 줄어듦",
                "활동이 늘어남", "예민/공격적", "숨는 시간이 많음", "야옹거림 증가"
            ],
            default=["평소와 비슷"]
        )

        symptoms = st.multiselect(
            "⚠️ 이상 증세",
            ["구토", "기침", "호흡 이상", "다리를 절음",
             "눈/코 분비물", "가려움/과도한 그루밍", "기타"]
        )

        memo = st.text_area("📝 특이사항 메모 (선택)",
            placeholder="평소와 달랐던 점, 먹은 간식, 환경 변화 등")

    if st.button("기록 저장"):
        rec = {
            "날짜": rec_date,
            "식사량": meal,
            "음수량": water,
            "배변": poop,
            "행동": ", ".join(activity),
            "이상증세": ", ".join(symptoms),
            # ⭐ 구토 저장
            "구토 여부": vomit_yes,
            "구토 색": vomit_color,
            "구토 종류": vomit_type,
            "구토 양": vomit_amount,
            "구토 횟수": vomit_count,
            "메모": memo,
        }
        add_record(rec)
        st.success("기록이 저장되었습니다!")

    st.markdown("---")
    st.subheader("📚 최근 기록")

    df = get_records_df()
    if df.empty:
        st.info("아직 기록이 없습니다.")
    else:
        st.dataframe(df.tail(10), use_container_width=True)


# -----------------------------
# 페이지 2. AI 진단 (구토 분석 추가)
# -----------------------------
def page_ai_diagnosis():
    st.title("📊 AI 진단 (프로토타입)")

    df = get_records_df()
    if df.empty:
        st.info("진단을 위해 최소 1개 이상의 기록이 필요합니다.")
        return

    recent = df.tail(7)

    warnings = []
    notes = []

    # ▫ 식사량
    if (recent["식사량"].isin(["거의 안 먹음", "평소보다 적게"])).sum() >= 2:
        warnings.append("식사량 감소가 반복적으로 보입니다.")
        notes.append("24시간 이상 거의 먹지 않으면 병원 상담 필요.")

    # ▫ 음수량
    if (recent["음수량"].isin(["거의 안 마심", "평소보다 적게"])).sum() >= 2:
        warnings.append("물 섭취량이 적은 날이 여러 번 있습니다.")
        notes.append("방광염·신장 문제의 초기 신호일 수 있습니다.")

    # ▫ 배변
    if (recent["배변"].isin(["무름/설사", "혈변/검은 변", "안 봄"])).sum() >= 2:
        warnings.append("배변 상태 이상이 반복됩니다.")
        notes.append("사진 기록 후 3일 이상 지속 시 병원 필요.")

    # ▫ 행동
    if recent["행동"].str.contains("활동이 줄어듦|잠이 많아짐|숨는", na=False).sum() >= 2:
        warnings.append("무기력/숨기 등 스트레스 신호가 발견됩니다.")

    # ⭐ 구토 분석 추가
    vomit_records = recent[recent["구토 여부"] == "있음"]

    if len(vomit_records) >= 2:
        warnings.append("최근 며칠간 구토가 여러 번 기록되었습니다.")

        types = " ".join(vomit_records["구토 종류"])
        colors = " ".join(vomit_records["구토 색"])

        if "헤어볼" in types:
            notes.append("헤어볼 구토가 잦으면 빗질·헤어볼 케어가 도움 됩니다.")
        if "거품" in types or "물토" in types:
            notes.append("허기성 구토일 수 있어요. 적은 양을 나눠 급여하면 좋아요.")
        if "이물" in types:
            warnings.append("이물 섭취 가능성이 있어 즉시 병원 권장.")
        if "분홍" in colors or "붉은" in colors:
            warnings.append("혈성 구토 의심 → 즉시 병원 방문 필요.")
        if (vomit_records["구토 횟수"] == "4회 이상").sum() >= 1:
            warnings.append("오늘 구토 횟수가 많아 탈수 위험이 있습니다.")

    if not warnings:
        st.success("최근 기록에서 큰 위험 신호는 감지되지 않았어요.")
    else:
        st.subheader("⚠️ 주의가 필요한 변화")
        for w in warnings:
            st.warning("- " + w)

    if notes:
        st.subheader("💡 참고 팁")
        for n in notes:
            st.write("- " + n)

    st.markdown("---")
    with st.expander("최근 7개 기록 보기"):
        st.dataframe(recent, use_container_width=True)


# -----------------------------
# 페이지 3. 집사 가이드 (기존 그대로)
# -----------------------------
def page_guide():
    st.title("📚 집사 가이드")
    st.write("고양이 집사가 꼭 알아두면 좋은 내용을 모아두었어요.")

    tabs = st.tabs(
        [
            "초보 집사 기본 가이드",
            "약 먹이기",
            "격리·적응",
            "목욕",
            "발톱 관리",
            "노령묘 관리",
            "중성화 수술 이후",
            "다이어트/체중 관리",
            "대표 질병 징후",
        ]
    )

    with tabs[0]:
        st.write("- 숨을 수 있는 공간 제공")
        st.write("- 밥·물·화장실 위치 고정")
        st.write("- 환경 변화 최소화")
        st.write("- 스트레스 완화")

    with tabs[1]:
        st.write("- 약 보조 간식 활용")
        st.write("- 알약은 혀 뒤편에 조심히 넣기")

    with tabs[2]:
        st.write("- 문 사이로 냄새 적응 후 대면")

    with tabs[3]:
        st.write("- 필요할 때만 목욕")

    with tabs[4]:
        st.write("- 혈관(분홍 부분) 피해서 조금씩 자르기")

    with tabs[5]:
        st.write("- 관절·신장 관련 질환 관리 중요")

    with tabs[6]:
        st.write("- 수술 부위 핥음 방지")
        st.write("- 중성화 이후 살찌기 쉬움 → 사료 조절")

    with tabs[7]:
        st.write("- 급여량 조절 & 사냥놀이로 활동량 증가")

    with tabs[8]:
        st.write("- 방광염, 장염, 구강 문제 등 주요 증상 체크")


# -----------------------------
# 페이지 4. 응급상황 AI (기존 그대로)
# -----------------------------
def page_ai_emergency():
    st.title("🚨 응급상황 AI")
    st.write("기본 응급 대응 안내입니다.")
    st.write("※ 본 기능은 실제 의료 행위를 대체하지 않습니다.")


# -----------------------------
# 페이지 5. 음식 사전 (기존 유지)
# -----------------------------
def page_food_dict():
    st.title("🍙 음식 사전")

    food_db = st.session_state.food_db

    query = st.text_input("음식 이름을 입력하세요")

    if st.button("검색"):
        q = query.strip()
        if not q:
            st.warning("먼저 음식 이름을 입력하세요.")
        elif q in food_db:
            info = food_db[q]
            if info["가능"] == "불가":
                st.error(f"❌ {q} : 고양이에게 절대 급여하면 안 됩니다.")
            else:
                st.success(f"⭕ {q} : {info['가능']}")
            st.write("주의사항:", info["주의"])
        else:
            st.warning(f"{q} 는 데이터에 없습니다.")

    with st.expander("등록된 음식 목록 보기"):
        df = pd.DataFrame(
            [
                {"음식": k, "급여 가능": v["가능"], "주의": v["주의"]}
                for k, v in food_db.items()
            ]
        )
        st.table(df)


# -----------------------------
# 페이지 6. 마켓 (기존 유지)
# -----------------------------
def page_market():
    st.title("🛍 마켓 (프로토타입)")
    st.write("건강 기록 기반 간단 추천 기능입니다.")
    st.write("※ 실제 구매 기능은 포함되어 있지 않습니다.")


# -----------------------------
# 라우팅
# -----------------------------
menu = st.sidebar.radio(
    "메뉴",
    ["홈", "건강 기록", "AI 진단", "집사 가이드", "응급상황 AI", "음식 사전", "마켓"],
)

if menu == "홈":
    st.title("🐱 묘멘트")
    st.write("고양이의 작은 변화를 기록하고 분석해주는 웹앱입니다.")

elif menu == "건강 기록":
    page_health_log()

elif menu == "AI 진단":
    page_ai_diagnosis()

elif menu == "집사 가이드":
    page_guide()

elif menu == "응급상황 AI":
    page_ai_emergency()

elif menu == "음식 사전":
    page_food_dict()

elif menu == "마켓":
    page_market()
